---
interface Founder {
  name: string;
  role: string;
  headline?: string;
  image: string;
  summary: string;
  paragraphs: string[];
}

interface Props {
  founder: Founder;
}

const { founder } = Astro.props;
---

<div class="founder-bio">
  <div class="founder-bio__copy">
    <p class="eyebrow">A bit about me</p>
    <h2 class="section-title">{founder.headline ?? founder.name}</h2>
    <p class="founder-role">{founder.role}</p>
    <p class="founder-summary">{founder.summary}</p>
    {founder.paragraphs.map((paragraph) => <p>{paragraph}</p>)}
  </div>

  <div class="about-portrait">
    <img src={founder.image} alt={founder.name} />
  </div>
</div>
